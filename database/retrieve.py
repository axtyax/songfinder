

#retrieve 20 random songs
def retrieve_songs():
    return #return a list of tuples structured as such:
    #(song name, length, views, thumbnail_url, url, UniqueID32bytes, None)
    #the last 'None' term will store associations between this song and others for use in prediction